# About


* lambda expressions
* object-oriented approach
* stream, IntStream, DoubleStream, LongStream
* unit tests
* Getting to know the functions:

     - stream (),
     - filter (),
     - map (),
     - collect ()
     - joining(),
     - flatMap()
     - reduce(),